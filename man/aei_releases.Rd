% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/releases.R
\name{aei_releases}
\alias{aei_releases}
\title{List available Anthropic Economic Index releases}
\usage{
aei_releases(live = TRUE)
}
\arguments{
\item{live}{Logical. If \code{TRUE} (the default), query Hugging Face for
the current set of release directories and merge with the bundled
metadata. If \code{FALSE}, return the bundled list only.}
}
\value{
An \link{aei_tbl} with columns \code{release_id}, \code{release_date},
\code{model}, and \code{notes}.
}
\description{
Queries the Hugging Face dataset listing for the Anthropic Economic
Index and returns one row per release, augmented with the headline
Claude model and a short note when the release is recognised. When
the network is unavailable (or \code{live = FALSE}), the function returns
the bundled list of releases known at package build time.
}
\examples{
\donttest{
op <- options(aieconindex.cache_dir = tempdir())
aei_releases()
aei_releases(live = FALSE)
options(op)
}
}
\seealso{
Other release discovery: 
\code{\link{aei_files}()}
}
\concept{release discovery}
