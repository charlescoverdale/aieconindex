% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labor_market.R
\name{aei_labor_market}
\alias{aei_labor_market}
\title{Fetch the AEI labor market impacts tables}
\usage{
aei_labor_market(
  table = c("job_exposure", "task_penetration"),
  use_cache = TRUE
)
}
\arguments{
\item{table}{Character. Either \code{"job_exposure"} (the default) or
\code{"task_penetration"}.}

\item{use_cache}{Logical. If \code{TRUE} (the default), use the local
cache when present.}
}
\value{
An \link{aei_tbl} containing the requested table.
}
\description{
Alongside the dated releases, the Anthropic Economic Index publishes
a standalone \code{labor_market_impacts} directory on Hugging Face with
two tables from Anthropic's labor market research: occupation-level
job exposure and task-level penetration. This function fetches
either table. The directory keeps Anthropic's US spelling.
}
\details{
The two tables are:
\itemize{
\item \code{"job_exposure"}: occupation-level exposure of jobs to AI
usage, keyed on SOC occupation codes.
\item \code{"task_penetration"}: task-level penetration of AI usage,
keyed on O*NET task identifiers.
}
Unlike the dated releases, this directory is not versioned by date;
Anthropic may update it in place. Clear the cache with
\code{\link[=aei_cache_clear]{aei_cache_clear()}} (or call with \code{use_cache = FALSE}) to force a
fresh download. See
\url{https://www.anthropic.com/research/labor-market-impacts} for
the accompanying research.
}
\examples{
\donttest{
op <- options(aieconindex.cache_dir = tempdir())
exposure <- aei_labor_market("job_exposure")
head(exposure)
options(op)
}
}
\seealso{
Other core data: 
\code{\link{aei_clusters}()},
\code{\link{aei_download}()},
\code{\link{aei_geography}()},
\code{\link{aei_index}()},
\code{\link{aei_tasks}()}
}
\concept{core data}
