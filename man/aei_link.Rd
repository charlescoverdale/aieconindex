% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{aei_link}
\alias{aei_link}
\title{Join an Anthropic Economic Index table to your own data}
\usage{
aei_link(
  x,
  y,
  by,
  type = c("left", "inner", "full"),
  suffixes = c(".aei", ".y")
)
}
\arguments{
\item{x}{An \link{aei_tbl} returned by any data-fetching function.}

\item{y}{A \code{data.frame} (or \code{aei_tbl}) to join.}

\item{by}{Character vector of column names present in both \code{x} and
\code{y}. Pass a named vector (e.g. \code{c(cluster_name = "onet_id")}) to
join columns with different names.}

\item{type}{One of \code{"left"} (the default; keep all rows of \code{x}),
\code{"inner"} (keep only matched rows), or \code{"full"} (keep all rows of
both, fill with \code{NA}).}

\item{suffixes}{Character vector of length two giving suffixes to
append to overlapping column names.}
}
\value{
An \link{aei_tbl} with the joined columns. Provenance metadata
from \code{x} is preserved.
}
\description{
Generic merge helper that preserves the \link{aei_tbl} class and
provenance metadata. Use it to splice the AEI to any external data
frame on a shared key column, for example joining country-level
AEI shares to working-age employment counts from a national
labour-force survey, or joining O*NET task identifiers to a
user-supplied occupational crosswalk (SOC to ANZSCO, SOC to ISCO,
SOC to SOC2020 UK, etc.).
}
\details{
The function is a thin wrapper over \code{\link[base:merge]{base::merge()}} with two
differences. First, it preserves the \code{aei_tbl} class and the
\code{aei_query} provenance attribute on the returned object so that
downstream code can still see where the AEI side of the join came
from. Second, it warns when a join produces zero rows, which is
usually a sign of a key mismatch (typed differently, different
code system, or different case).

For occupational crosswalks: the long-format AEI schema (from
2025-09-15 onwards) carries the O*NET task identifier in the
\code{cluster_name} column when \code{facet == "onet_task"}, and SOC major
group codes appear in \code{cluster_name} when \code{facet == "onet_task"}
and \code{variable == "soc_pct"}. See \code{data_documentation.md} in each
release on Hugging Face for the authoritative schema.

For country joins: country codes are ISO-3 in the enriched data
(\code{"GBR"}, \code{"AUS"}, \code{"USA"}). If your external data uses ISO-2 codes,
map them first with a small lookup table or with the
\pkg{countrycode} package on CRAN.
}
\examples{
\donttest{
op <- options(aieconindex.cache_dir = tempdir())
# Join AEI country shares to a small external table of GDP per capita
country <- aei_geography("2025-09-15")
overlay <- data.frame(
  geo_id = c("GBR", "AUS", "USA"),
  gdp_pc = c(48000, 65000, 80000)
)
joined <- aei_link(country, overlay, by = "geo_id")
head(joined)
options(op)
}
}
\seealso{
Other analysis: 
\code{\link{aei_compare}()},
\code{\link{aei_concentration}()}
}
\concept{analysis}
